export {};
// export {} - Pongo esto porque TypeScript se estaba confundiendo y pensaba 
// que esta función estaba duplicada por culpa del otro archivo JS. Con esto 
// le aviso a TS que este archivo es independiente y se arregla el error.
